# Deep_learn_final
# 🎬 Anime Popularity/Score Prediction with Multimodal Deep Learning  
> ทำนายความนิยมของอนิเมะจากข้อมูลหลายมิติ  

รายงานวิชา: **การเรียนรู้เชิงลึก (Deep Learning)**  
รหัสวิชา: 204466  
ผู้จัดทำ: นายกวิน ชุนงาม (6610505241)  
คณะวิศวกรรมศาสตร์ สาขาวิศวกรรมคอมพิวเตอร์  
อาจารย์ผู้สอน: อาจารย์ภารุจ รตันวรพันธ์  

---

## 1️⃣ บทนำ (Introduction)

ในปัจจุบัน อุตสาหกรรมอนิเมะมีการผลิตผลงานจำนวนมากในแต่ละปี  
โดยมีความหลากหลายทั้งด้านแนวเรื่อง (Genre), สตูดิโอผู้ผลิต, จำนวนตอน และรูปแบบการนำเสนอ  
ทำให้การคาดการณ์ความนิยม (Popularity) หรือคะแนนความพึงพอใจ (Score) ของอนิเมะเป็นเรื่องที่ซับซ้อน  
และยากต่อการประเมินล่วงหน้า เนื่องจากปัจจัยที่มีผลต่อความนิยม  
ไม่ได้อยู่เพียงในข้อมูลเชิงตัวเลขเท่านั้น แต่ยังรวมถึง **เนื้อหาและอารมณ์ของเรื่อง (Synopsis)**  
ซึ่งเป็นข้อมูลเชิงข้อความที่มีความหมายลึกซึ้งและซับซ้อน

เพื่อแก้ปัญหานี้ โครงการนี้จึงเลือกใช้แนวทาง  
**Deep Learning แบบ Multimodal** ที่ผสานข้อมูลทั้งสองประเภท คือ  

- **ข้อมูลเชิงข้อความ (Textual Features):** ชื่อเรื่องและคำบรรยาย (Synopsis)  
  → ใช้โมเดลภาษาขนาดใหญ่ (DistilBERT) เพื่อเข้าใจความหมายของภาษา  
- **ข้อมูลเชิงตาราง (Tabular Features):** ประเภทของอนิเมะ (Genres), สตูดิโอ, และจำนวนตอน  
  → ใช้เครือข่าย Tabular MLP เพื่อเรียนรู้ความสัมพันธ์เชิงตัวเลข  
- **Fusion MLP:** ผสานข้อมูลทั้งสองด้านเข้าด้วยกันเพื่อทำนายคะแนนความนิยมของอนิเมะ

---

## 2️⃣ ที่มาและเหตุผล (Motivation)

### 2.1 การเปรียบเทียบกับวิธีดั้งเดิม
แนวทางเดิมมักใช้การแปลงข้อความเป็นเวกเตอร์แบบง่าย เช่น **Bag-of-Words** หรือ **TF-IDF**  
และใช้โมเดลอย่าง **SVM** หรือ **Random Forest** สำหรับข้อมูลเชิงตารางแยกกัน  
ซึ่งมีข้อจำกัด เช่น  
- ไม่เข้าใจความหมายของคำ เช่น *“action”* กับ *“battle”*  
- ไม่สามารถเชื่อมโยงข้อมูลจากหลายแหล่งได้  
- เมื่อมีคำใหม่หรือชื่อเฉพาะ โมเดลไม่สามารถจัดการได้ดี  

### 2.2 ข้อดีของ Deep Learning
Deep Learning สามารถเรียนรู้ได้จากทั้งข้อมูลข้อความและตารางพร้อมกัน  
โดยไม่ต้องออกแบบ Feature ด้วยมือ  

- **DistilBERT:** เข้าใจบริบทและอารมณ์ของภาษา  
- **MLP:** เรียนรู้ความสัมพันธ์เชิงตัวเลข เช่น แนวเรื่องกับสตูดิโอ  
- **Fusion Layer:** รวมผลลัพธ์ทั้งสองเพื่อทำนายคะแนนอย่างแม่นยำ  

---

## 3️⃣ วิธีการที่นำเสนอ (Proposed Method)

### 3.1 สถาปัตยกรรมของโมเดล

#### 🧩 3.1.1 Baseline Model — Text Embedding + MLP
**Input:**  
- Text Embedding จาก `all-MiniLM-L6-v2`  
- Genres → Multi-hot encoding  
- Studio → Embedding  
- Episodes → Scaled numeric  

**Architecture:**  
```
Concatenate → Dense(256, ReLU) → Dropout → Dense(128, ReLU) → Dense(1)
```

#### ⚙️ 3.1.2 Advanced Model — DistilBERT + Tabular Fusion
- Text branch: DistilBERT → ใช้ [CLS] token (768 hidden size)  
- Tabular branch: concat(episodes_scaled, genre_mh, studio_emb) → MLP(128 units)  
- Fusion: Concat([CLS], tab_repr) → Dense(256, ReLU) → Dropout → Dense(1)  
- Trainable: DistilBERT + Fusion head (ใช้ LR แยก text และ head)

---

### 3.2 ขั้นตอนการฝึก (Training Process)

#### 3.2.1 การเตรียมข้อมูล (Data Preparation)
- แบ่งข้อมูลเป็น **Training set (tr)** และ **Validation set (va)**  
- เข้ารหัสข้อมูลหมวดหมู่:  
  - `genre_enc` → multi-hot  
  - `studio_enc` → embedding index  
- มาตรฐานข้อมูลตัวเลข (`episodes`)  
- สำหรับข้อความ:  
  - Baseline → Sentence Transformer  
  - Advanced → DistilBERT Tokenizer

---

#### 3.2.2 Baseline Model Training
- Optimizer: `AdamW (lr=1e-3)`  
- Scheduler: `CosineAnnealingLR`  
- Loss: `MSELoss`  
- Metrics: MAE, RMSE, SpearmanR  
- Gradient clipping: max_norm=5.0  
- Save best model based on lowest RMSE  

---

#### 3.2.3 Advanced Model Training
- Learning Rate แยก:
  - DistilBERT → `1e-5`
  - MLP Head → `1e-4`
- Optimizer: `AdamW`
- Scheduler: `CosineAnnealingLR`
- Loss: `MSELoss`
- Train ทั้ง DistilBERT และ Fusion Layer
- ประเมินและบันทึกโมเดลที่ RMSE ต่ำสุด

---

## 4️⃣ หลักการที่ใช้ประเมินผล (Evaluation Metrics)

### 🔹 MAE (Mean Absolute Error)
ค่าเฉลี่ยของความคลาดเคลื่อนแบบสัมบูรณ์ระหว่างค่าทำนายกับค่าจริง  
> ยิ่งค่าน้อย โมเดลยิ่งแม่นยำ  

### 🔹 RMSE (Root Mean Squared Error)
รากที่สองของค่าเฉลี่ยกำลังสองของความคลาดเคลื่อน  
> ให้ความสำคัญกับ error ขนาดใหญ่  

### 🔹 Spearman’s Rank Correlation (ρ)
วัดความสัมพันธ์เชิงลำดับระหว่างค่าทำนายกับค่าจริง  
> ค่ายิ่งใกล้ 1 แสดงว่าโมเดลจัดอันดับได้ถูกต้อง  

---

## 5️⃣ การประเมินผลและวิเคราะห์ (Evaluation & Discussion)

| Metric | Baseline | Advanced |
|:--|:--:|:--:|
| **MAE** | 0.483 | 0.526 |
| **RMSE** | 0.619 | 0.674 |
| **SpearmanR** | 0.756 | 0.698 |

### 🔍 วิเคราะห์ผล
1. **Baseline Model ดีกว่าในทุกตัวชี้วัด**  
   เนื่องจากใช้ Sentence Transformer ที่ฝังเวกเตอร์คงที่และเสถียรกว่า  
2. **Advanced Model มีศักยภาพสูงกว่าแต่ต้องจูนมากกว่า**  
   Fine-tuning BERT ต้องใช้ข้อมูลจำนวนมากและ LR ที่ละเอียด  
3. **ชุดข้อมูลมีเพียง ~10k ตัวอย่าง** ทำให้ DistilBERT ยังเรียนรู้บริบทไม่เพียงพอ  

---

### ✅ สรุปผลการประเมิน
- **Baseline Model** มีประสิทธิภาพดีกว่า (MAE, RMSE, SpearmanR ต่ำกว่า)  
- **Advanced Model** แม้ซับซ้อนกว่าแต่ยังไม่ถึงศักยภาพสูงสุด  
- แสดงให้เห็นว่า “โมเดลใหญ่ไม่จำเป็นต้องให้ผลลัพธ์ดีกว่าเสมอไป”  
  หากขนาดข้อมูลและพารามิเตอร์ไม่เหมาะสม  

---

## 📚 สรุปภาพรวม
> โครงงานนี้แสดงให้เห็นถึงประสิทธิภาพของ Deep Learning แบบ Multimodal  
> ที่สามารถผสานข้อมูลข้อความและเชิงตารางในการทำนายความนิยมของอนิเมะได้อย่างมีเหตุผล  
> โดยโมเดลพื้นฐาน Mixed-modal MLP ให้ผลลัพธ์ที่ดีที่สุดในชุดข้อมูลปัจจุบัน  
> และมีแนวทางพัฒนาเพิ่มเติมด้วยการเพิ่มข้อมูลและปรับจูนโมเดลภาษาในอนาคต
